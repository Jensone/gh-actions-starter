name: gh-action-starter
description: Workflow de test avec GitHub action pour expérimenter le service
author: Jensone
version: 1.0.0
branding:
  icon: 'docker'
  color: 'gray-dark'

runs:
  using: 'docker'
  image: 'Dockerfile'
